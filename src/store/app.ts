import { defineStore } from 'pinia';

export const useAppStore = defineStore('app', () => {
  // This store has been refactored. Logic moved to salary.ts, settings.ts, and ui.ts

  return {
    // Empty store
  };
});